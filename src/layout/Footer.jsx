import React from 'react';
import PropTypes from 'prop-types';

const Footer = (props) => {
  return (
    <div>
      Copy
    </div>
  );
};

Footer.propTypes = {};
Footer.defaultProps = {};

export default Footer;
